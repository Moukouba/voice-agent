[project]
name = "livekit-voice-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "livekit>=1.1.14",
    "livekit-agents[silero,turn-detector]>=1.7.0",
    "livekit-plugins-noise-cancellation>=0.3.0",
    "python-dotenv>=1.2.3",
]
